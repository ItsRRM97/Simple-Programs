#include<stdio.h>

struct node //linear linked list's node is defined 
{
	int data;
	struct node *next; // self referencing pointer
}

void create() // to dynamically allocate memory to a single linked list
{
	
}
